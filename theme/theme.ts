import { createTheme } from '@mui/material/styles'

const theme = createTheme({
  palette: {
    mode: 'dark',
    primary: {
      main: '#2e7d32',
    },
    secondary: {
      main: '#ffcc00',
    },
  },
})

export default theme